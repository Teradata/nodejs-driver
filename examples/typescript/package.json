{
  "name": "examples-typescript",
  "description": "node.js teradata driver example using typescript",
  "version": "1.0.0-alpha.1",
  "dependencies": {
    "teradata-nodejsdriver": "1.0.0-beta.1"
  },
  "devDependencies": {
    "@types/node": "^10.5.7",
    "rimraf": "2.6.2",
    "ts-node": "~7.0.0",
    "tslint": "~5.11.0",
    "typescript": "^3.0.1",
    "typescript-formatter": "^7.2.2"
  },
  "scripts": {
    "clean": "rimraf dist",
    "lint": "tslint ./**/*.ts",
    "format": "tsfmt -r",
    "build": "npm run clean && tsc --outDir dist src/*.ts"
  }
}